# FIX (SCA / Trivy): версии подняты до закрывающих найденные CVE,
# неиспользуемые приложением пакеты (Django, paramiko, SQLAlchemy,
# requests и их транзитивные зависимости) удалены — это убирает
# критические находки и сокращает attack surface.
Flask==3.0.3
Werkzeug==3.0.6
Jinja2==3.1.5
itsdangerous==2.2.0
click==8.1.7
gunicorn==23.0.0
PyYAML==6.0.2
MarkupSafe==2.1.5
